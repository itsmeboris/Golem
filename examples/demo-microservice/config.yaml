# Golem config for the demo project — conservative budget
flows:
  golem:
    profile: local
    budget_per_task_usd: 5.0
    task_timeout_seconds: 600
    max_retries: 1
    supervisor_mode: true
    task_model: sonnet
    orchestrate_model: opus
    use_worktrees: true
