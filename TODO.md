# TODO

## Active

| #   | Status | Task                                                                                                                                                                                                          | Impact | Priority     |
| --- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------ |
| 26  | [ ]    | **Worktree and data isolation** — `create_worktree` and `merge_in_worktree` write to `data/agent/` under `base_dir`, which pollutes the repo root during production runs; gitignore `data/` at root, and ensure all tests use `tmp_path` instead of the real repo for worktree/merge operations | High   | P0 (immediate) |
| 27  | [ ]    | **Post-merge re-verification** — run black/pylint/pytest after merge to catch regressions introduced by conflict resolution; fail merge if checks don't pass                                                  | High   | P1           |
| 28  | [ ]    | **Dashboard prompt comparison UI** — table or chart view for `/api/analytics/by-prompt` data (API exists, frontend missing; GH #2 acceptance criteria gap)                                                    | Low    | P3           |
| 21  | [ ]    | **State management audit rule** — detect innerHTML replacement without state preservation, polling without concurrency guards, shared mutable state in async code                                              | Medium | P2           |
| 22  | [ ]    | **Codebase contract linting** — static check that function return types match consumer expectations (e.g. list vs string) across module boundaries                                                            | Medium | P2           |

## Completed

| #   | Task                                                                                                                                                                                                                                                                     | Impact | Priority |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | -------- |
| 13  | **Prompt versioning & analytics** — hash prompts into `RunRecord`, add `/api/analytics/by-prompt` endpoint (API only; dashboard UI tracked in #28)                                                                                                                       | Medium | P3       |
| 20  | **Integration smoke tests as quality gate** — FastAPI TestClient integration tests for health, submit, dashboard, ping, and flow/status endpoints; `@pytest.mark.integration` marker                                                                                     | High   | P1       |
| 10  | **Task replay + dashboard controls** — re-run, edit-and-resubmit modal, cancel button wired to UI in task detail view                                                                                                                                                    | Medium | P2       |
| 1   | **Batch task submission** — declare and submit individual tasks as part of a named batch (each task addressable by name/ID), with a global orchestrator that monitors the batch, tracks progress, aggregates results, and reports overall batch status                     | High   | P1       |
| 2   | **Structured validation feedback loop** — when retrying on PARTIAL, pass structured feedback (specific files to fix, test failures, concrete concerns) instead of free-text summary                                                                                       | High   | P1       |
| 3   | **Post-merge sanity check** — after merge, verify agent branch additions survive in the merged result — `verify_merge_integrity` runs inside the isolated merge worktree                                                                                                 | High   | P1       |
| 4   | **Health monitoring & alerting** — daemon health metrics (heartbeat, queue depth, error rate, disk usage) and optional alerting (Slack/Teams) when the daemon is stuck or degraded                                                                                        | High   | P1       |
| 17  | **Isolated merge worktree** — temporary worktree for all merge operations, never touching the user's working tree; unified merge agent for conflict resolution; deferred merge support                                                                                    | High   | P1       |
| 5   | **GitHub Issues backend profile** — `github` profile (TaskSource, StateBackend, Notifier) using `gh` CLI — enables adoption by non-Redmine users                                                                                                                        | High   | P2       |
| 8   | **Checkpoint-based recovery on agent failures** — save intermediate state and resume from last good checkpoint when the Claude CLI crashes mid-task (OOM, network, context overflow)                                                                                      | Medium | P2       |
| 9   | **Antipattern rules for validation** — checks for traceback leaks, private attribute access across modules, string-matching for control flow (in validation prompt and as static analysis)                                                                                | Medium | P2       |
| 15  | **Stop task from dashboard** — cancel API endpoint (`POST /api/cancel/{task_id}`) wired to `cancel_session()` flow (note: dashboard button UI still pending, tracked in #10)                                                                                             | Medium | P2       |
| 16  | **First-run config wizard** — interactive `golem init` command: profile selection, model/budget choices, notification setup, work directory mapping, generates ready-to-use `config.yaml`                                                                                 | High   | P2       |
| 11  | **Cost analytics & budget insights** — aggregate `cost_usd` into dashboard: cost over time, cost by verdict, cost per retry, budget utilization trends                                                                                                                   | Medium | P3       |
| 12  | **Pipeline flow graph for task dashboard** — ELK.js dependency DAG, live monitoring bar, sortable task table, expandable event detail with full trace data                                                                                                                | Low    | P3       |
| 18  | **Live status watch mode** — `golem status --watch` for auto-refreshing terminal output and `golem status --task <id>` for per-task detail view                                                                                                                          | Medium | P3       |
| 14  | **Task prompt template** — instruct agents to use `@pytest.mark.parametrize` instead of duplicating test methods that differ by one variable                                                                                                                             | Low    | P4       |
| 7   | **Agent context injection** — auto-inject AGENTS.md + CLAUDE.md into agent sessions as system prompt context; write-back mechanism for agents to persist discoveries into AGENTS.md                                                                                       | High   | P1       |
| 19  | **Expanded antipattern detection** — add validation rules for hardcoded UI states, dead/unreachable code, data contract mismatches (backend returns X, frontend expects Y), missing error/edge-case handling                                                              | High   | P1       |
| 6   | **Adoption & outreach plan** — strategy to advertise, gather feedback, and grow the user base                                                                                                                                                                            | High   | P2       |
| 23  | **Post-task learning loop** — after each completed task, extract what the validator/verifier caught and append to AGENTS.md as "known pitfalls" so future agents avoid the same mistakes                                                                                  | Medium | P2       |
| 24  | **Validator fix-cycle depth** — allow validator to produce testable fix items and loop (build→review→fix) up to N iterations within a single task, rather than one-shot retry                                                                                              | Medium | P3       |
| 25  | **SSE-based dashboard live updates** — replace 5s polling with Server-Sent Events (SSE) for real-time dashboard updates; emit events from supervisor milestones and trace writer appends                                                                                  | Medium | P3       |
