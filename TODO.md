# TODO


| #   | Status | Task                                                                                                                                                                                                                                                                     | Impact | Priority |
| --- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | -------- |
| 1   | [x]    | **Batch task submission** — allow declaring and submitting individual tasks as part of a named batch (each task addressable by name/ID), with a global orchestrator agent that monitors the batch, tracks progress, aggregates results, and reports overall batch status | High   | P1       |
| 2   | [x]    | **Structured validation feedback loop** — when retrying on PARTIAL, pass structured feedback (specific files to fix, test failures, concrete concerns) instead of free-text summary — reduces wasted retry cycles                                                        | High   | P1       |
| 3   | [x]    | **Post-merge sanity check** — after merge_and_cleanup, verify agent branch additions survive in the merged result — manual commits on master can silently overwrite agent work during rebase                                                                             | High   | P1       |
| 4   | [x]    | **Health monitoring & alerting** — add daemon health metrics (heartbeat, queue depth, error rate, disk usage) and optional alerting (Slack/Teams ping) when the daemon is stuck or degraded                                                                              | High   | P1       |
| 5   | [x]    | **GitHub Issues backend profile** — implement a `github` profile (TaskSource, StateBackend, Notifier) using `gh` CLI or GitHub API — enables adoption by non-Redmine users and lowers the barrier to entry                                                               | High   | P2       |
| 6   | [ ]    | **Adoption & outreach plan** — create a strategy to advertise and push this git repo to more users — gather feedback, drive interaction, and grow the user base                                                                                                          | High   | P2       |
| 7   | [ ]    | **Agent memory / learning across tasks** — persist cross-task context per repo (e.g. an `AGENTS.md`-style file with discovered conventions, test frameworks, project quirks) so agents don't start from scratch every time                                               | Medium | P2       |
| 8   | [x]    | **Checkpoint-based recovery on agent failures** — when the Claude CLI crashes mid-task (OOM, network, context overflow), save intermediate state and resume from last good checkpoint instead of failing the whole session      | Medium | P2       |
| 9   | [x]    | **Antipattern rules for validation** — add checks (in validation prompt or as static analysis) for traceback leaks in responses, private attribute access across modules, string-matching for control flow, etc.                                                         | Medium | P2       |
| 10  | [ ]    | **Task replay / re-run from dashboard** — add a "re-run" button (and edit-and-resubmit) to the task detail view, turning the dashboard into a full control plane rather than read-only monitoring                                                                        | Medium | P3       |
| 11  | [ ]    | **Cost analytics & budget insights** — aggregate `cost_usd` data into a dashboard view — cost over time, cost by task type, cost per retry, budget utilization trends                                                                                                    | Medium | P3       |
| 12  | [x]    | **Pipeline flow graph for task dashboard** — replaced sidebar + flat list with ELK.js dependency DAG, live monitoring bar, sortable task table, and expandable event detail with full trace data                                                                         | Low    | P3       |
| 13  | [ ]    | **Prompt versioning & A/B testing** — track which prompt version produced which results, enabling systematic comparison of prompt changes rather than eyeballing outcomes                                                                                                | Low    | P3       |
| 14  | [x]    | **Task prompt template** — instruct agents to use `@pytest.mark.parametrize` instead of duplicating test methods that differ by one variable                                                                                                                             | Low    | P4       |
| 15  | [ ]    | **Stop task from dashboard** — add a stop/cancel button to the task detail view and DAG node context, wiring it to the existing `POST /api/cancel/{task_id}` endpoint so users can kill running tasks without the CLI                                                    | Medium | P2       |
| 16  | [ ]    | **First-run config wizard** — interactive `golem init` command that guides new users through configuration: profile selection, model/budget choices, notification setup, work directory mapping, and generates a ready-to-use `config.yaml` with recommendations           | High   | P2       |


